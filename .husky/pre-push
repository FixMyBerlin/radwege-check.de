#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# This is `bun run check` without knip/format (faster push).
# Use `git push --no-verify` to force-skip them.
bun run type-check
bun run lint
bun run test
